YOU are the implementer for Ferrum Edge issues #3267 and #3268, covering multiple Gateway `certificateRefs` on one listener and removal of the one-certificate-source-per-namespace data-plane limit. Work only in the supplied isolated worktree on branch `opus/issues-3267-3268-multi-cert-r1`, based on current `origin/main`.

Read both complete issues and comments with `gh issue view 3267` and `gh issue view 3268`, then read all repository/path-scoped instructions and the Gateway API, TLS/security, secrets, config, admin/spec, and testing docs before editing. Implement both issues completely and cohesively: strict cross-namespace authorization, correct SNI certificate selection and fallback behavior, reload/delete/rotation, collision and ownership semantics, bounded caches without namespace aliasing, redaction, status conditions, docs/OpenAPI parity, and focused translation plus live TLS coverage. Fail closed on invalid or unauthorized references and never leak key material.

ABSOLUTE LOCAL MACHINE PROHIBITION: never run local Rust/build/test/toolchain commands: no `cargo`, `rustc`, `rustfmt`, or `clippy`; no builds, tests, benchmarks, servers, coverage, or `protoc`; no project executables; no project/repository scripts; and do not invoke any of these through git hooks. GitHub-hosted CI is the only execution, formatting, lint, and test gate. Allowed locally: static source inspection, ordinary `git`/`gh` inspection, synthetic git merge inspection that does not modify protected work, and `git diff --check`.

YOU are the implementer; write, commit, and push directly. Do not dispatch nested workers. Do not merge. Do not post `@codex review` anywhere, whether as a top-level PR comment or an inline thread reply. Do not request or shepherd Codex review; this explicitly overrides any normal review-trigger guidance. Use `--no-verify` for commits and pushes. Never reset, rebase, force-push, clean, delete, discard, or overwrite unexplained work. Use a normal latest-main merge if compatibility changes are needed.

Create one ready PR targeting `main` only if both issues are fully resolved, with a precise Summary/Changes/Test plan and both `Closes #3267` and `Closes #3268`. Push the exact branch, report the commit SHA and PR URL, and exit after pushing and reporting rather than waiting indefinitely for CI.
