{{- if .Values.eastWest.enabled }}
{{- $tcpHandler := dict "tcpSocket" (dict "port" "tls-passthru") -}}
{{- $liveHandler := .Values.eastWest.probes.liveness.override | default dict -}}
{{- if not $liveHandler }}{{- $liveHandler = $tcpHandler -}}{{- end -}}
{{- $readyHandler := .Values.eastWest.probes.readiness.override | default dict -}}
{{- if not $readyHandler }}{{- $readyHandler = $tcpHandler -}}{{- end -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ferrum-mesh-east-west
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: ferrum-mesh-east-west
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: {{ .Values.eastWest.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: ferrum-mesh-east-west
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ferrum-mesh-east-west
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      serviceAccountName: ferrum-mesh
      containers:
        - name: ferrum-edge
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args: ["run"]
          ports:
            - name: tls-passthru
              containerPort: {{ .Values.eastWest.listenPort }}
          env:
            - name: FERRUM_MESH_EAST_WEST_LISTEN_PORT
              value: {{ .Values.eastWest.listenPort | quote }}
            {{- range $name, $value := .Values.eastWest.env }}
            {{- if ne $name "FERRUM_MESH_EAST_WEST_LISTEN_PORT" }}
            - name: {{ $name }}
              value: {{ $value | quote }}
            {{- end }}
            {{- end }}
          {{- include "ferrum-mesh.renderProbes" (dict "probes" .Values.eastWest.probes "liveHandler" $liveHandler "readyHandler" $readyHandler) }}
---
apiVersion: v1
kind: Service
metadata:
  name: ferrum-mesh-east-west
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app.kubernetes.io/name: ferrum-mesh-east-west
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: tls-passthru
      port: {{ .Values.eastWest.listenPort }}
      targetPort: tls-passthru
{{- end }}
